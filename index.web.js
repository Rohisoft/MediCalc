import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('MediPlus', () => App);
AppRegistry.runApplication('MediPlus', {
  rootTag: document.getElementById('root'),
});
